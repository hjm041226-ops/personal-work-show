# 公共站前端环境变量示例（复制为 .env.development / .env.production 按需修改）

# 后端 API baseURL（公开组前缀 /api/v1，由契约固定）
# - 不设置时默认 '/api/v1'：适用于“前端与后端同源 / 由网关/反向代理转发”的部署
# - 跨域直连时填完整地址（含 /api/v1），例如：
VITE_API_BASE_URL=/api/v1
# VITE_API_BASE_URL=https://personal-work-backend.onrender.com/api/v1
